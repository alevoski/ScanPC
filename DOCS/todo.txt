- Create process tree with parent/children
- List tasks (schtasks)

- KB list : order by date

- Find a way to removed the 2 system calls left
    verif.py
        line 25 os.system('cls')
        line 60 subprocess.call(cmdlineUser, cwd=dir, shell=True, stdout=subprocess.PIPE, stdin=subprocess.PIPE, stderr=subprocess.PIPE)

- Good html indent
